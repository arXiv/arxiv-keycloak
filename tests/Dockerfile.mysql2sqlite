FROM python:3.12-slim

# Install mysql-to-sqlite3
RUN pip install --no-cache-dir mysql-to-sqlite3

# Set working directory
WORKDIR /data

# Default entrypoint runs mysql2sqlite with passed arguments
ENTRYPOINT ["mysql2sqlite"]
